The <info>%command.name%</info> command generates a new specification.

<info>php %command.full_name% IsDummy</info>

If the argument is missing, the command will ask for the class name interactively.
